apiVersion: v1
kind: ConfigMap
metadata:
  name: centraldashboard-config
data:
  # empty line to maintain scalar style after kustomize
  settings: |-

    {{ toPrettyJson .Values.settings | indent 4 }}
  # empty line to maintain scalar style after kustomize
  links: |-

    {{ toPrettyJson .Values.links | indent 4 }}
